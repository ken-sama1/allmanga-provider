export type Scalars =
	| "BigInt"
	| "Boolean"
	| "DateTime"
	| "Float"
	| "ID"
	| "Int"
	| "Object"
	| "String"
	| "Upload";

export type DateTime =
	`${number}-${number}-${number}T${number}:${number}:${number}Z`;
